# 4871 그래프 경로

import sys
sys.stdin = open("input.txt")

for tc in range(int(input())):
    node_num, route_num = map(int, input().split())
    routes = sorted([list(map(int, input().split())) for _ in range(route_num)])
    start, end = map(int, input().split())
    stack = []
    
    while stack:
        if :
            
    

    # print(f'#{tc+1} {ans}')
